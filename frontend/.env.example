# Copy this file to .env.local for local development.
# In production (Vercel), set these in your project's Environment Variables dashboard.

# URL of the backend API (deployed on Railway / Render / Fly.io)
# Leave empty to use localhost:4000 in local dev
VITE_API_URL=https://your-backend.railway.app/api
